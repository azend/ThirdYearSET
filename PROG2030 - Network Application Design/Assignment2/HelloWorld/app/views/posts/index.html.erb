<div id="posts">
	<% for post in @posts do %>
		<div class="post">
			<h3><%= post.title %></h3>
			<%= post.body %>
			<p>Post was written on <%= post.created_at %> and last updated at <%= post.updated_at %></p>
		</div>
	<% end %>
</div>
